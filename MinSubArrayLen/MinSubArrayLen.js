const minSubArrayLen = (arr, target) => {
    let left = 0;
    let right = 1;
    let len = Infinity;

    while (right < arr.length) {
        


    }
};

console.log(minSubArrayLen([2, 3, 1, 5, 2, 3], 7)); // 2
