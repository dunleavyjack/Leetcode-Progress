# Notes

This is can be solved using an iterative or recursive method. For the recursive
method, the main idea is to swap the children of each node, and then call the
function recursively on both the left and right sides. The base case is to
ensure that a node exists.

| Time | Space |
| ---- | ----- |
| 0(n) | 0(h)  |
