# Solution

This one is easier than I thought! The idea is to create two pointers at either
end of the `heights` array. Calculate the total area that point, and then move
whichever pointer's value is smaller inward. That's it!
